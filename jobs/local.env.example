# SQUID 固有の設定。グループ名とメールアドレスは利用者ごとに違うため、
# ジョブスクリプトには書かず、このファイルのコピーに書く。
#
#   cp jobs/local.env.example jobs/local.env
#   vi jobs/local.env
#
# jobs/local.env は .gitignore 対象なので、リポジトリには入らず pull でも衝突しない。
#
# GROUP はフロントエンドで `groups` を実行して確認する。
GROUP="<グループ名>"
MAIL="<メールアドレス>"
