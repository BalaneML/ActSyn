# コンテナ内へ追加インストールする依存。
# バージョンは uv.lock（ローカル環境の解決結果）に一致させて固定する。
#
# 意図的に除外しているもの:
#   torch  : NGC イメージ同梱の A100 最適化ビルドを使う。pip で上書きしない。
#   numpy  : NGC 同梱版と ABI を揃える。pandas 側の要求で必要なら pip が自動解決する。
#
# 追加・変更したら container/domaintransfer.def の再ビルドが必要。

pandas==3.0.3
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.10.9
japanize-matplotlib==1.1.3
openpyxl==3.1.5
prince==0.19.0
umap-learn==0.5.12
pyyaml==6.0.3
tqdm==4.68.2
wandb==0.28.0
